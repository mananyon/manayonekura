# CLAUDE.md — 米倉まな プロフィールサイト

このファイルは Claude Code が最初に読む作業コンテキストです。

## このリポジトリは何か

メンタル鍼灸師・米倉まな（よねくら まな, 1983年生まれ・三重県出身）の
実績プロフィール1ページサイト。営業資料・メディア向けの自己紹介として使う。

**ビルドツールなし。`index.html` を直接編集する。**
フレームワーク・npm・バンドラは一切使っていない。使わないでほしい。

## 絶対に守ること

1. **CSSは原則インラインstyle属性**。`<head>` の `<style>` に入っているのは
   グローバルリセット・スマホ用メディアクエリ・`:hover` 用クラス（`.hv1`〜`.hv12`）だけ。
   新しいクラスベースのCSS設計に作り替えないこと。
2. **本文テキストは勝手に書き換えない**。米倉さん本人の言葉で書かれている。
   「生きづらさ」「よりそう」などの平易な語彙は意図的なもの。
   漢字を増やしたり、より「プロっぽい」表現に直したりしない。
3. **画像は生成できない**。新しい写真が必要なら本人に依頼する。
4. **数字は事実**。11年目 / のべ20,000人+ / 3事業 / 修士（武蔵野大学大学院 人間学）。
   更新は本人確認を取ってから。
5. **外部リンクは全て実在するURL**。`docs/CONTENT.md` に一覧がある。追加・変更時はそこも更新。

## デザイントークン

| 用途 | 値 |
|---|---|
| 背景（ベース） | `#ECE3D4` |
| カード背景 | `#FCFAF4` / `#FBF7F0` |
| リンク帯背景 | `#E4D9C6` |
| 濃色帯（Stats / Research） | `#2B2620` |
| 本文テキスト | `#2B2620` / `#46403A` / `#4A4239` |
| 補助テキスト | `#8B8175` / `#9A9082` |
| 罫線 | `#D6C9B6` / `#D2C5B0` |
| アクセント（朱） | `#9C5446` — CSS変数 `--accent` |
| アクセント2（深緑） | `#5E7060` — CSS変数 `--accent2` |
| アクセント3（金） | `#C8A06A` / `#E8C9A8`（濃色背景上の見出し） |
| 見出しフォント | `'Noto Serif JP', serif` weight 600 |
| 本文フォント | `'Noto Sans JP', sans-serif` weight 400/500 |
| コンテンツ幅 | `max-width: 1080px` / `padding: 88px 56px` |
| 角丸 | カード `12px` / 画像 `8px` |

CSS変数 `--accent` `--accent2` はルート `<div>` のstyle属性で定義。
配下では `var(--accent, #9C5446)` の形で参照している。フォールバック付きなので
変数定義を消してもデザインは崩れない。

## セクション構成（上から順）

各 `<section>` に `data-screen-label` が付いている。これで grep すると速い。

| label | 内容 |
|---|---|
| `Hero` | 氏名・肩書きタグ4つ・ポートレート |
| `Stats` | 濃色帯、数字4つ |
| `Story` | 患者から鍼灸師へ。闘病の経緯 |
| `Clinic Night` | 夜の院外観、フルブリード写真帯 |
| `Activities` | 3事業カード（臨床 / サロン / 育成）＋ MEMBER'S CLINICS リンク |
| `Research` | 濃色帯。大学院・学会・論文（Google Scholar リンク） |
| `Media` | メディア掲載5件のリスト ＋ YouTubeインタビュー埋め込み |
| `Contribution` | 社会貢献カード3枚 |
| `Links` | 3サイトへのリンクカード ＋ LINE CTA 2つ ＋ フッター |

## スマホ対応

`<head>` 内 `@media (max-width: 760px)` の1ブロックのみ。
`[style*="..."]` の属性セレクタでインラインstyleを上書きしている。
やや変則的だが、インラインstyle方針とレスポンシブを両立させるための意図的な選択。
**セクションを追加したらこのメディアクエリも確認すること**（特にgridは1カラムに落とす）。

## よくある依頼と対応箇所

- 「実績を1件追加」→ `data-screen-label="Media"` の `<a>` をコピーして追記
- 「色味を変えたい」→ ルート `<div>` の `--accent` / `--accent2` を変更
- 「写真を差し替え」→ `images/` に同名で上書き、または `src` を変更
- 「動画を変えたい」→ `Media` セクションの iframe `src` の動画ID部分
- 「肩書きを追加」→ `Hero` のタグ `<span>` をコピー

## デプロイ

`main` に push → GitHub Pages が自動反映（1〜2分）。ビルドステップなし。

## 元データ

このサイトは Claude の Design Component（`.dc.html`）から静的HTMLに書き出したもの。
元ファイルは `src/profile.dc.html` に参考として置いてあるが、**これは編集対象ではない**。
今後の編集は `index.html` に直接行う。
